#include "main.h"
/**
 *add - a function that adds two integers and returns the result 
 *Return: alphabet
 */
void print_alphabet(void)
{
	int n;

	for (n = 'a'; n <= 'z'; n++)
	{
		_putchar(n);
	}
	_putchar('\n');
}
