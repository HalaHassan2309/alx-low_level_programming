#include "main.h"

/**
 * _islower - a function that prints the alphabet ten times
 *
 * Return: Always 0 (Succes)
 */
int _islower(int c)
{
	if (c >= 'a' || c <= 'z')
	{
		return (1);
	} else 
	{
		return (0);
	}
}
