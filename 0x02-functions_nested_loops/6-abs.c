#include "main.h"

/**
 * _abs - return 1 char 'c' is letter
 * @n : the input
 * Return: Always 0 (Succes)
 */
int _abs(int n)
{
        if (n > 0)
        {
		return (n);
        } else if (n < 0)
                return (-1*n);
	}
        return (0);
}
